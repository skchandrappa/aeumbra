[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "cd pythonbackend && pip install --upgrade pip",
  "cd pythonbackend && pip install -r requirements.txt"
]

[start]
cmd = "cd pythonbackend && uvicorn main:app --host 0.0.0.0 --port $PORT"
workDir = "pythonbackend"

